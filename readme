基于形式化验证的列控系统安全移动权限分析:基于模型检查器MCMAS的ISPL代码

SMA的安全决策过程简化如下。假设火车有两种状态：继续行驶和停车等待。当列车申请SMA时，
首先需要检查影响列车运行的各种因素，包括系统设备的受控变量，即下一区间的占用和空闲，
以及周围环境的不可控变量，即是否发生了地震或滑坡，以及是否有异物侵入等。这里列出了
以上三个典型的周围环境不可控变量来表示列车的简化运行场景。当所有影响因素都正常时，生
成一个可信的SMA，并将其分配给当前列车；否则不产生SMA。
